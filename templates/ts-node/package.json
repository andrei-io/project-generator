{
  "name": "my_node_app",
  "version": "0.0.1",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node build/index.js",
    "dev": "tsc --watch & nodemon build"
  },
  "dependencies": {},
  "devDependencies": {}
}
